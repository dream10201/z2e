# 只在导出 INT4 IR 时需要；运行时不用
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
optimum==2.3.0
optimum-intel[openvino]==2.1.0
nncf==3.3.0
# 版本卡在两头中间：optimum-intel 锁 transformers<5.6；同时它的 Qwen3.5 patcher
# 要 import Qwen3_5DynamicCache，这个类只存在于 transformers 5.2.0-5.4.0
# （5.5.0 起被移除，optimum-intel 没跟上，见 optimum-intel#1786），
# 所以取窗口内最新的 5.4.0。Hy-MT2（hunyuan_v1_dense）模型卡推荐 >=5.6 只是推荐，
# 5.5.4 实测能加载；5.4.0 待真机回归确认
transformers==5.4.0
