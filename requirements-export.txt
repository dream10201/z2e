# 只在导出 INT4 IR 时需要；运行时不用
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
optimum==2.3.0
optimum-intel[openvino]==2.1.0
nncf==3.3.0
# optimum-intel（含 git main）锁 transformers<5.6，5.5.4 是约束内最新，
# Hy-MT2（hunyuan_v1_dense）实测能加载。注意：Qwen3.5 系导出用不了这个版本——
# optimum-intel 的导出配置把它硬锁在 transformers==5.2.0（见 optimum-intel#1786），
# 真要导 Qwen3.5 就临时降到 5.2.0
transformers==5.5.4
