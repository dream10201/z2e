name: build-images

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        target: [runtime, export]

    steps:
      - uses: actions/checkout@v5

      - name: 腾出磁盘空间
        if: matrix.target == 'export'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          df -h /

      - uses: docker/setup-buildx-action@v3

      - name: 登录 GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 生成 tag
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE }}
          # runtime -> :runtime / :latest；export -> :export
          flavor: |
            latest=false
          tags: |
            type=raw,value=${{ matrix.target }}
            type=raw,value=latest,enable=${{ matrix.target == 'runtime' }}
            type=ref,event=tag,suffix=-${{ matrix.target }}
            type=sha,format=short,suffix=-${{ matrix.target }}

      - name: 构建并推送
        uses: docker/build-push-action@v6
        with:
          context: .
          target: ${{ matrix.target }}
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          load: ${{ github.event_name == 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.target }}
          cache-to: type=gha,mode=max,scope=${{ matrix.target }}

      - name: 冒烟测试
        run: |
          IMG=$(printf '%s\n' '${{ steps.meta.outputs.tags }}' | head -1)
          # 绕过 entrypoint 的模型检查：runner 上没有模型，也不该在这儿下 15GB
          docker run --rm -v "$PWD/ci:/ci:ro" --entrypoint bash "$IMG" /ci/smoke.sh

      - name: entrypoint 在模型缺失时给出可操作的提示
        run: |
          IMG=$(printf '%s\n' '${{ steps.meta.outputs.tags }}' | head -1)
          set +e
          out=$(docker run --rm -e AUTO_EXPORT=0 "$IMG" python -c "print('不该跑到这')" 2>&1)
          code=$?
          set -e
          echo "$out"
          test $code -ne 0 || { echo "模型缺失时应该退非零"; exit 1; }
          grep -q "找不到模型" <<<"$out" || { echo "没给出提示信息"; exit 1; }
